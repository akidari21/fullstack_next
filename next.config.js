/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.pravatar.cc', 'www.valleywestvets.com'],
  },
};

module.exports = nextConfig;
